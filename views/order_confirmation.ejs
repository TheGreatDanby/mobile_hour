<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include("partials/head.ejs") %>
    <title>The Mobile Hour - Order Confirmation</title>
  </head>
  <body>
    <main>
      <%- include("partials/header.ejs") %>
      <section class="checkout-layout">
       

            <article class="card lg:card-side bg-accent shadow-xl">
                <img class="p-10" src="img/mobile_phone.png" alt="Mobile Phone" />
                <div class="card-body text-white">
                  <span class="card-title"><%- order_with_product.product_manufacturer %></span>
                  <span class="card-title"><%- order_with_product.product_name %></span>
                  <span class="font-black text-2xl tracking-wider text-right">
                    $<%- order_with_product.product_price %>
                  </span>
                </div>
              </article>
<div class="grid grid-cols-2 gap-5">
          <label for="time">Order Time:</label>
          <span id="status"
            ><%= (new Date (order_with_product.order_date)).toLocaleString()
            %></span
          >
          <label for="name">Order Name:</label>
          <span id="name"
            ><%= order_with_product.order_customer_first_name %></span
          >
          <label for="status">Order Status:</label>
          <span id="status"><%= order_with_product.order_status %></span>
          <a href="/" class="btn btn-primary">Back</a>
          <a
            href="order_confirmation?id=<%= order_with_product.order_id %>"
            class="btn btn-secondary"
            >Refresh</a
          >
        </div>
        </article>
      </section>
      <%- include("partials/footer.ejs") %>
    </main>
  </body>
</html>
